package toolkit

// JSON-Schema keys shared by the toolkit tool descriptors. Centralized so
// goconst stays satisfied and a rename reflects across every descriptor.
const (
	_schemaKeyType        = "type"
	_schemaKeyDescription = "description"
	_schemaTypeString     = "string"
)
